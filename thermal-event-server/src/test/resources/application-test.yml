# 자동 테스트 전용 설정: 실제 MySQL 대신 메모리 DB(H2, MySQL 호환 모드)를 사용합니다.
spring:
  datasource:
    url: jdbc:h2:mem:thermal;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
logging:
  file:
    name: target/test.log
